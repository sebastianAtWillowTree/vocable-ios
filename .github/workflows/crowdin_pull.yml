name: Pull Translations From Crowdin
on:
  push:
    branches:
      - develop
  workflow_dispatch:
    inputs:
      runner:
        description: 'Runner to use'
        required: false
        default: 'macos-latest'
        type: choice
        options:
          - macos-latest
          - self-hosted

jobs:
  synchronize-with-crowdin:
    runs-on: ${{ inputs.runner || vars.RUNNER_TYPE || 'macos-latest' }}
    steps:

      - name: Debug - Show runner info
        run: |
          echo "=== Runner Information ==="
          echo "Runner: ${{ runner.name }}"
          echo "OS: ${{ runner.os }}"
          echo "Architecture: ${{ runner.arch }}"
          echo "Temp directory: ${{ runner.temp }}"
          echo "Workspace: ${{ github.workspace }}"
          echo "=========================="
        shell: sh

      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Install Crowdin CLI
        run: |
          if ! command -v crowdin &> /dev/null; then
            echo "Installing Crowdin CLI..."
            brew install crowdin
          else
            echo "Crowdin CLI already installed"
          fi
        shell: sh

      - name: Set Project ID as Integer
        run: |
          echo "CROWDIN_PROJECT_ID=$((CROWDIN_PROJECT_ID))" >> $GITHUB_ENV
        env:
          CROWDIN_PROJECT_ID: ${{ vars.CROWDIN_PROJECT_ID }}
        
      - name: Download Translations
        run: |
          # TODO: Update bundle ID to your xcstrings bundle ID in Crowdin
          # Current bundle 13 is configured for XLIFF export
          # Create a new bundle in Crowdin configured for xcstrings export and update the ID below
          crowdin bundle download 13 --project-id=834156 --token=84079ca8898375f652f3723b83c4da9cbdefdcd551841902bafa2341fc518efb686878a66ebb3f60
        shell: sh
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            CROWDIN_PROJECT_ID: 834156
            CROWDIN_PERSONAL_TOKEN: 84079ca8898375f652f3723b83c4da9cbdefdcd551841902bafa2341fc518efb686878a66ebb3f60

      - name: Debug - Check downloaded files
        run: |
          echo "=== Checking if CrowdinExport directory exists ==="
          if [ -d "CrowdinExport" ]; then
            echo "✓ CrowdinExport directory exists"
            echo ""
            echo "=== Directory structure ==="
            ls -R CrowdinExport/
            echo ""
            echo "=== XLIFF file count ==="
            find CrowdinExport -name "*.xliff" | wc -l
            echo ""
            echo "=== xcstrings file count ==="
            find CrowdinExport -name "*.xcstrings" | wc -l
            echo ""
            echo "=== Sample file content (first file found) ==="
            first_file=$(find CrowdinExport \( -name "*.xliff" -o -name "*.xcstrings" \) -print -quit)
            if [ -n "$first_file" ]; then
              echo "File: $first_file"
              head -50 "$first_file"
            else
              echo "No XLIFF or xcstrings files found"
            fi
          else
            echo "✗ CrowdinExport directory does NOT exist"
            echo "Bundle download may have failed or returned no files"
          fi
        shell: sh

      - name: Set up Ruby environment
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.3.1
          
      - name: Install bundler
        run: gem install bundler
            
      - name: Install dependencies
        run: bundle install
    
      - name: Install SwiftLint
        run: |
          if ! command -v swiftlint &> /dev/null; then
            echo "Installing SwiftLint..."
            brew install swiftlint
          else
            echo "SwiftLint already installed"
          fi
        shell: sh

      - name: Import translations to project
        uses: ./.github/actions/run_lane
        with:
          # Use xcstrings_import for xcstrings bundles, xliff_import for XLIFF bundles
          lane: xliff_import

      - name: Debug - Check for changes after import
        run: |
          echo "=== Git diff summary ==="
          git diff --stat
          echo ""
          echo "=== Detailed changes in xcstrings files ==="
          git diff --name-only | grep -E '\.(xcstrings|strings)$' || echo "No xcstrings/strings files changed"
          echo ""
          echo "=== Full git status ==="
          git status
        shell: sh

      - name: Stage localization changes
        run: |
          echo "=== Files in CrowdinExport ==="
          ls -R CrowdinExport/
          echo ""
          echo "=== Git status before staging ==="
          git status
          echo ""
          echo "=== Looking for changed xcstrings/strings files ==="
          find Vocable -name "*.xcstrings" -o -name "*.strings" | head -20
          echo ""
          echo "=== Staging all changes in Vocable directory ==="
          git add Vocable/
          echo ""
          echo "=== Git status after staging ==="
          git status
        shell: sh

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Update Localizations
          committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          author: ${{ github.actor }} <${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com>
          signoff: false
          branch: crowdin/update-translations-bot
          base: develop
          delete-branch: true
          add-paths: |
            Vocable/**/*.xcstrings
            Vocable/**/*.strings
          title: '[CI] Update Localizations'
          body: |
            Automated localization update from Crowdin.
          labels: |
            localization
            automated
          draft: false
